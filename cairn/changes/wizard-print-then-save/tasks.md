---
cairn: tasks
id: wizard-print-then-save
---

# Tasks

- [x] Print the fragment before the save step in every mode; turn `save_or_print` into `offer_save`, taking the account by reference and no printer.
- [x] Reword the prompt to `Save this configuration to a file?`.
- [x] Confirm the append when the chosen file already holds something, naming the path.
- [x] Update the wizard module header, the README and the CHANGELOG.
- [x] Run `cargo fmt`, `cargo clippy` and `cargo test`.
- [x] Fold the delta into `cairn/spec/discovery.md`, write the log entry, set this change to landed.
